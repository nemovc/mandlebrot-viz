[package]
name = "mandelbrot"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
astro-float = "0.9"
getrandom = { version = "0.2", features = ["js"] }

[profile.release]
opt-level = 3
lto = true
